#include <stdio.h>
#include <math.h>
int main() 
{
    int n,arr[n],i,j,key;
    printf("enter the size of array");
    scanf("%d",&n);
    printf ("enter elements of array");
    for (i=0;i<n;i++)
    { 
        scanf("%d",&arr[i]);
    }
    for(i=1;i<n;i++)
    {
        key=arr[i];
        j=i-1;
    }
    while(j>=0&&arr[j]>key)
    {
        arr[j+1]=arr[j];
        j--;
    }
     arr[j+1]=key;

    printf("sorted array");
    for(i=0;i<n;i++)
    {
        printf("%d",arr[ i ]);
        }
} 
    
