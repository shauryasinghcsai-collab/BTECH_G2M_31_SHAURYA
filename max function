#include <stdio.h>

int main() {
     int m[100][100] ;
    int r, c,i,n ,j,max=m[0][0];
    printf("Enter number of rows and columns: ");
    scanf("%d %d", &r, &c);
    printf("\nEnter matrix elements:\n");
    for (int i = 0; i < r; ++i) {
        for (int j = 0; j < c; ++j) {
            printf("Enter element a%d%d: ", i + 1, j + 1);
            scanf("%d", &m[i][j]); }}
       
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            if (m[i][j]>max){
                max=m[i][j];
                
             }
        }
    }
            printf("maximum element is :- %d \t",max);
           
            return 0;
}
        
               
             
           
          
      
    
