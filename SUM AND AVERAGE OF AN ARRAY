#include <stdio.h>
int main() 
{
    int n,i,sum=0;
    float avg;
    printf("enter the size of array");
    scanf("%d",&n);
    int arr[n];
    printf("enter elements of array\n");
    for(i=1;i<=n;i++)
    { 
        printf("element %d:",i);
        scanf("%d",&arr[i]);
       
    }
     for(i=1;i<=n;i++)
    { 
     sum=sum+arr[i];
    }
    avg=(float)sum/n;
    printf("sum of array %d \n" ,sum);
    printf(" avrage of array %2f \n" ,avg);
    }
